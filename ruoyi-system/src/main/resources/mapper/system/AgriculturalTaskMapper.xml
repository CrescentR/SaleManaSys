<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.AgriculturalTaskMapper">

    <resultMap type="AgriculturalTask" id="AgriculturalTaskResult">
        <result property="id"    column="id"    />
        <result property="name"    column="name"    />
        <result property="description"    column="description"    />
        <result property="startDate"    column="start_date"    />
        <result property="endDate"    column="end_date"    />
    </resultMap>

    <sql id="selectAgriculturalTaskVo">
        select id, name, description, start_date, end_date from agricultural_task
    </sql>

    <select id="selectAgriculturalTaskList" parameterType="AgriculturalTask" resultMap="AgriculturalTaskResult">
        <include refid="selectAgriculturalTaskVo"/>
        <where>
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="description != null  and description != ''"> and description = #{description}</if>
            <if test="startDate != null "> and start_date = #{startDate}</if>
            <if test="endDate != null "> and end_date = #{endDate}</if>
        </where>
    </select>

    <select id="selectAgriculturalTaskById" parameterType="String" resultMap="AgriculturalTaskResult">
        <include refid="selectAgriculturalTaskVo"/>
        where id = #{id}
    </select>

    <insert id="insertAgriculturalTask" parameterType="AgriculturalTask" useGeneratedKeys="true" keyProperty="id">
        insert into agricultural_task
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">name,</if>
            <if test="description != null">description,</if>
            <if test="startDate != null">start_date,</if>
            <if test="endDate != null">end_date,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null">#{name},</if>
            <if test="description != null">#{description},</if>
            <if test="startDate != null">#{startDate},</if>
            <if test="endDate != null">#{endDate},</if>
        </trim>
    </insert>

    <update id="updateAgriculturalTask" parameterType="AgriculturalTask">
        update agricultural_task
        <trim prefix="SET" suffixOverrides=",">
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="startDate != null">start_date = #{startDate},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteAgriculturalTaskById" parameterType="String">
        delete from agricultural_task where id = #{id}
    </delete>

    <delete id="deleteAgriculturalTaskByIds" parameterType="String">
        delete from agricultural_task where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>